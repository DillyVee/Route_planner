# ============================================================================
# DRPP Route Planner - Dependency Installation Guide
# ============================================================================
#
# This project uses modern Python packaging with pyproject.toml (PEP 517/518).
# Please use one of the following installation methods instead of this file:
#
# For core library only (DRPP solver without GUI):
#   pip install -e .
#
# For GUI application (recommended for most users):
#   pip install -e ".[gui]"
#
# For all features:
#   pip install -e ".[all]"
#
# For development:
#   pip install -e ".[dev]"
#
# Individual feature groups:
#   pip install -e ".[viz]"        # Visualization only
#   pip install -e ".[ml]"         # Machine learning clustering
#   pip install -e ".[osm]"        # OSM speed integration
#   pip install -e ".[profiling]"  # Performance profiling tools
#
# ============================================================================
# See pyproject.toml for the complete dependency specification
# ============================================================================
